# Copy to .env and adjust as needed:
#   cp .env.example .env

NODE_ENV=production

# Used when building upstream Host headers from Docker
SWITCHBOARD_UPSTREAM_HOST=host.docker.internal

# Optional release metadata (shown in /api/version and dashboard footer)
# SWITCHBOARD_VERSION=1.0.0
# SWITCHBOARD_BUILD=local
# SWITCHBOARD_COMMIT=
