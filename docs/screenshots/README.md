# Screenshots

Add PNG/WebP captures here and reference them from the root `README.md`.

Suggested filenames:

- `dashboard.png` — routes management
- `ssl.png` — TLS / Let's Encrypt settings
- `settings.png` — app / users / backup

Until real screenshots exist, the README table will show broken image placeholders — that is intentional.
